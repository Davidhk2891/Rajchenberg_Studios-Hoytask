<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp"
    android:background="?android:attr/selectableItemBackground">

    <View
        android:id="@+id/item_set_imageview"
        android:background="@color/hoytask_shimmer_grey"
        android:layout_width="20dp"
        android:layout_height="20dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:contentDescription="@string/set_icon_in_list"/>

    <View
        android:id="@+id/item_set_title"
        android:layout_width="200dp"
        android:layout_height="20dp"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="12dp"
        android:background="@color/hoytask_shimmer_grey"
        app:layout_constraintStart_toEndOf="@id/item_set_imageview"
        app:layout_constraintTop_toTopOf="@id/item_set_imageview"
        app:layout_constraintBottom_toBottomOf="@id/item_set_imageview" />

</androidx.constraintlayout.widget.ConstraintLayout>